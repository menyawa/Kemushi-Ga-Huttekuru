2020/04/04
ゲームクリアを改良

BGMを追加

ゲーム終了の処理を仮実装

2020/04/03
スコアのテキストの色を白に変更

スタート地点のハイライトを消去
スタート地点を画面外に変更
毛虫のスポーン範囲、画面外判定を修正

Runnerのフィールドを微修正

スコアとゲージの位置を調整

スコアを仮実装(フォントが反映されていない？)

WindowSizeの関係の処理を分離

ブーストゲージの表示順を修正し、ゲージが毛虫で隠れないよう修正

スタート地点のハイライトを実装
それに伴い、その位置には毛虫が落ちないよう修正

Caterpillarのコンストラクタにコンストラクタ初期化子を使うよう修正

ブーストゲージをきちんと見えるよう修正

ダッシュ機能を個別クラスに分離

2020/04/02
ランナーがスタート地点から反対側の端に到達した際、反転する機能を追加

着地後振動する不具合を修正
スポーンの間隔を長くするよう修正

スポーンの間隔制御を仮実装

着地の判定、向き変更の処理を追加(毛虫のみ)
それに伴い、落下時、着地時の元画像番号を配列で管理する方式に変更

背景関連の処理を分離して、一つのクラスに修正

ブーストの挙動を修正(押しっぱなしの場合、即座にブーストを繰り返していた)

特定キーを押した際のブーストを仮実装
Runnerの一行メソッドをインライン関数化

2020/04/01
ポインタ類を全てスマートポインタ、あるいは変数に変更
Caterpillarのコンストラクタをprivateにし、randomSpawnでインスタンスを生成する方式に変更
つまるところ、乱数をより精度の高いものに変更

Caterpillarのリストをスマートポインタに変更

2020/01/20
Caterpillarの落下、右這い、左這いの画像を静的フィールド化(全て共通の画像を使っており、使うたびに無駄な負荷がかかると考えたため)
Caterpillarの静的フィールドの初期化メソッドを追加
左右それぞれの落下時、着地時の画像を追加

2020/01/18
Caterpillarに移動処理を追加

60FPSの動作に合わせての遅延処理を実装(Fpsクラス)

2020/01/17
GameControllerの初期化がDXライブラリの初期化より早く走っていたため、背景の初期化が出来ていなかった問題を修正

2020/01/15
ScreenFlipのタイミングを変更
backgroundColor, GroundColorを削除(Imageだけで事足りるため)、ゲッターも削除
backgroundImageHandle_, groundImageHandle_をフィールド化し、initWindowで初期化するよう修正(定数化しないのはLoadGraphがDXライブラリ初期化の後に実行しないと不具合が起きる恐れがあるため)
それに伴い、Runnerのmoveメソッドの引数を削除

Runner, GameControllerの変数名をリファクタリング、多少定数(const)化

Caterpillarの変数名を多少リファクタリング

毛虫のリサイズ・縦画像を追加
ランダムな位置に毛虫をスポーンするメソッドを追加
全てのインスタンスを削除する静的メソッドを追加

2020/01/14
Caterpillar(毛虫)クラスをとりあえず実装
矩形の当たり判定メソッドを実装

2020/01/05
ランナーを更に1/2の大きさに変更
移動距離を1に変更

背景描画関数を常に実行する形式に変更(裏画面初期化のため)

ランナーの移動の処理を追加
それに伴い、画像の描画を裏画面に設定する方式に変更

Runnerの画像を縮小

メインのプログラムをMainProgram→GameControllerクラスに移動
主要なフィールド、メソッドは全てそこに移動
backGroundColor, Pos, groundColor, Posを追加

メインループの仮実装
ProcessMessageの追加(バグの防止)

Runnerクラスの仮実装
コンストラクタ、getImageSizeX, getImageSizeYの実装

2020/01/04
画像の初期化

プロジェクト作成
ウィンドウモードや解像度など最低限の初期化